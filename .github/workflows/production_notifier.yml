name: Notifier

on:
  pull_request:
    branches: [ prod ]

jobs:

  messageSlack:
    runs-on: ubuntu-latest
    steps:
    - name: Notification Slack
      uses: rtCamp/action-slack-notify@v2
      env:
        SLACK_CHANNEL: bot_production
        SLACK_TITLE: "New PR"
        SLACK_USERNAME: CI
        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
        SLACK_MESSAGE: "Starting deploy 🚀🚀🚀"