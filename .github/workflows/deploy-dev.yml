# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy dev to Firebase Hosting

on:
  push:
    branches:
      - dev
      - fix/*

jobs:
  notify_start:
    runs-on: ubuntu-latest
    steps:
      - name: Notification Slack
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_CHANNEL: develop_tracker
          SLACK_TITLE: "Deployment"
          SLACK_USERNAME: (CI) Deployment
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_MESSAGE: The deploy for ${{github.ref_name}} will start soon 🚀🚀🚀

  deploy_dev:
    needs: [notify_start]
    runs-on: ubuntu-latest
    environment:
      name: dev
      url: https://dev.dibba.com.br
    steps:
      - uses: actions/checkout@v2
      - name: Checkout to the repository
        uses: actions/checkout@v2

      - name: Set up NodeJS environment
        uses: actions/setup-node@v2
        with:
          node-version: 18

      - name: Cache node modules
        id: cache-npm
        uses: actions/cache@v3
        env:
          cache-name: cache-node-modules
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: Install package dependencies
        run: npm install

      - name: Deployment
        run: npm run build_dev
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_FRONTEND_COSTUMER }}"
          projectId: frontend-costumer
          channelId : live
          target: dev
  notify_end:
    runs-on: ubuntu-latest
    needs: [deploy_dev]
    steps:
      - name: Notification Slack
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_CHANNEL: develop_tracker
          SLACK_TITLE: "Deployment"
          SLACK_USERNAME: (CI) Deployment
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_MESSAGE: The deploy for ${{github.ref_name}} was a success! 🎉🎉🎉
